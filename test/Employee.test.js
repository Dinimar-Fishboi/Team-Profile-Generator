const Employee = require("../lib/Employee");

describe("Employee Class", () => {
    //Then this is where all the BS is.
    // it('Every intern added generates a new card' () => {
    //     expect(new Intern())
    // });

})